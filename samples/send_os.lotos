process os_send[class.send, class.sent, class.notsent] :=
    os.req ?msg;
    class.send !msg;
    (
        class.sent;
        os.complete;
        exit
    []
        class.notsent ?err;
        os.failed !err;
        exit
    )
endproc
